<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="team3.promans.interfaces.SelectInterface">
	<select id="getCalendar" resultType="team3.promans.beans.Notice_CalendarBean" parameterType="team3.promans.beans.Notice_CalendarBean">
		SELECT TO_CHAR(PR_SDATE,'YYYYMMDD') AS SDATE,
		TO_CHAR(PR_LDATE,'YYYYMMDD') AS LDATE,
		CM.CM_USERID AS USERID 
		FROM PR INNER JOIN CM ON PR.PR_CPCODE = CM.CM_CPCODE 
		WHERE CM.CM_USERID = #{writer}
	</select>

</mapper>